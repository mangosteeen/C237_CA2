<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Task Filter</title>
  <!-- Bootstrap CDN -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-KyZXEJv+u8u2bZanF5by5Gn1XbxG5mndXg/1fbtGdu6eL4uY+VYTP4iHLhmyf7pZ" crossorigin="anonymous">
  <style>
    body {
      background-color: #f4f4f9; /* Light background color */
      margin: 0;
    }

    .hero-section {
      text-align: center;
      padding: 80px 0; /* Increased padding for more height */
      background-color: #6c63ff; /* Purple background */
      color: white; /* White text */
      border-bottom: 4px solid #ddd; /* Bottom border for separation */
      box-shadow: 0 10px 20px rgba(0, 0, 0, 0.1); /* Subtle shadow */
    }

    .hero-section h1 {
      font-size: 3rem; /* Larger font size for the title */
      font-weight: bold; /* Make title bold */
    }

    .hero-section p {
      font-size: 1.5rem; /* Larger font size for the description */
      margin-top: 20px; /* Add space between the title and description */
    }

    .filter-container {
      margin-top: 60px;
      padding: 20px;
      text-align: center;
    }

    .filter-container h2 {
      color: #6c63ff;
      font-size: 2rem;
    }

    .filter-container form {
      margin-top: 20px;
      display: flex;
      justify-content: center;
      gap: 10px;
      flex-wrap: wrap;
    }

    .filter-container select, .filter-container input {
      padding: 8px 15px;
      border-radius: 20px;
      border: 1px solid #ddd;
    }

    .filter-container button {
      background-color: #6c63ff;
      color: white;
      border: none;
      border-radius: 20px;
      padding: 10px 15px;
      font-size: 1rem;
    }

    .clear-btn {
      background-color: #ccc; /* Grey color for the clear button */
      color: black;
      border: none;
      border-radius: 20px;
      padding: 8px 15px;
      font-size: 1rem;
      cursor: pointer;
    }

    .task-list {
      margin-top: 40px;
      display: flex;
      justify-content: center;
      flex-wrap: wrap;
    }

    .task-card {
      background-color: #fff;
      border: 1px solid #ddd;
      margin: 15px;
      padding: 20px;
      border-radius: 10px;
      width: 300px;
      text-align: center;
    }

    .task-card h5 {
      color: #6c63ff;
    }

    .task-card p {
      color: #555;
    }

  </style>
</head>
<body>

  <!-- Hero Section -->
  <div class="hero-section">
    <h1>Welcome to Elderly Scheduler</h1>
    <p>Filter and manage your tasks efficiently by selecting the appropriate criteria.</p>
  </div>

  <!-- Filter Section Below Hero -->
  <div class="container filter-container">
    <h2>Filter Tasks</h2>
    <form method="GET" action="/" class="d-flex justify-content-center gap-3">
      <input class="form-control me-2" type="search" name="title" placeholder="Search by Task" value="<%= title || '' %>" aria-label="Search" id="searchField">
      <button class="clear-btn" type="button" onclick="clearSearch()">Clear</button>
      <select class="form-select me-2" name="urgency">
        <option value="">Urgency</option>
        <option value="Low" <%= urgency === 'Low' ? 'selected' : '' %>>Low</option>
        <option value="Medium" <%= urgency === 'Medium' ? 'selected' : '' %>>Medium</option>
        <option value="High" <%= urgency === 'High' ? 'selected' : '' %>>High</option>
      </select>
      <select class="form-select me-2" name="type">
        <option value="">Task Type</option>
        <option value="Medication" <%= type === 'Medication' ? 'selected' : '' %>>Medication</option>
        <option value="Exercise" <%= type === 'Exercise' ? 'selected' : '' %>>Exercise</option>
        <option value="Appointment" <%= type === 'Appointment' ? 'selected' : '' %>>Appointment</option>
      </select>
      <button class="btn btn-outline-light" type="submit">Filter</button>
    </form>

    <!-- Displaying Filtered Tasks as Cards -->
    <div class="task-list">
      <% tasks.forEach(task => { %>
        <div class="task-card">
          <h5 class="card-title"><%= task.title %></h5>
          <p><strong>Urgency:</strong> <%= task.urgency %></p>
          <p><strong>Type:</strong> <%= task.type %></p>
        </div>
      <% }) %>
    </div>
  </div>

  <!-- Bootstrap JS and Popper.js (optional) -->
  <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.10.2/dist/umd/popper.min.js" integrity="sha384-pzjw8f+ua7Kw1TIq0Rk3U09pB1zO1S6qD6PkkDl5XB3w4J0l6iUtC1LgQkfo5GEL" crossorigin="anonymous"></script>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.min.js" integrity="sha384-pzjw8f+ua7Kw1TIq0Rk3U09pB1zO1S6qD6PkkDl5XB3w4J0l6iUtC1LgQkfo5GEL" crossorigin="anonymous"></script>

  <!-- Clear Search Functionality -->
  <script>
    function clearSearch() {
      document.getElementById('searchField').value = ''; // Clears the search input field
    }
  </script>

</body>
</html>
