<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Add New Request | Elderly Assistance</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
  <style>
    body {
      background-color: #f8f9fa;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }
    .container {
      max-width: 500px;
      margin: 80px auto;
      background: white;
      padding: 40px;
      border-radius: 8px;
      box-shadow: 0 0 15px rgba(0, 0, 0, 0.1);
    }
    h1 {
      font-size: 2rem;
      font-weight: 600;
      margin-bottom: 30px;
      color: #495057;
      text-align: center;
    }
    .form-control {
      border-radius: 8px;
    }
    .btn-primary {
      background-color: #007bff;
      border-color: #007bff;
      font-weight: 600;
      border-radius: 8px;
      width: 100%;
      padding: 12px;
    }
    .btn-primary:hover {
      background-color: #0056b3;
      border-color: #004085;
    }
    .alert {
      margin-bottom: 20px;
    }
    .form-group label {
      font-weight: 500;
      color: #495057;
    }
    .mb-4 {
      margin-bottom: 1.5rem;
    }
  </style>
</head>
<body>

  <div class="container">
    <h1>Add New Request</h1>

    <% if (typeof errors !== 'undefined' && errors.length > 0) { %>
      <div class="alert alert-danger">
        <% errors.forEach(function(error) { %>
          <p><%= error %></p>
        <% }) %>
      </div>
    <% } %>

    <% if (typeof messages !== 'undefined' && messages.length > 0) { %>
      <div class="alert alert-success">
        <% messages.forEach(function(message) { %>
          <p><%= message %></p>
        <% }) %>
      </div>
    <% } %>

    <form action="/addNewRequest" method="POST">

      <div class="mb-3">
        <label for="name" class="form-label">Elderly's Name</label>
        <input type="text" class="form-control" id="name" name="name" required value="<%= formData ? formData.name : '' %>"/>
      </div>

      <div class="mb-3">
        <label for="taskType" class="form-label">Task Type</label>
        <select class="form-control" id="taskType" name="taskType" required>
          <option value="">-- Select Task Type --</option>
          <option value="Grocery Shopping" <%= formData && formData.taskType === 'Grocery Shopping' ? 'selected' : '' %>>Grocery Shopping</option>
          <option value="Medication Pickup" <%= formData && formData.taskType === 'Medication Pickup' ? 'selected' : '' %>>Medication Pickup</option>
          <option value="House Cleaning" <%= formData && formData.taskType === 'House Cleaning' ? 'selected' : '' %>>House Cleaning</option>
          <option value="Other" <%= formData && formData.taskType === 'Other' ? 'selected' : '' %>>Other</option>
        </select>
      </div>

      <div class="mb-3">
        <label for="description" class="form-label">Description</label>
        <input type="text" class="form-control" id="description" name="description" required value="<%= formData ? formData.description : '' %>"/>
      </div>

      <div class="mb-4">
        <label for="urgency" class="form-label">Urgency</label>
        <select class="form-control" id="urgency" name="urgency" required>
          <option value="">-- Select Urgency --</option>
          <option value="Low" <%=formData && formData.urgency==='Low' ? 'selected' : '' %>>Low</option>
          <option value="Medium" <%=formData && formData.urgency==='Medium' ? 'selected' : '' %>>Medium</option>
          <option value="High" <%=formData && formData.urgency==='High' ? 'selected' : '' %>>High</option>
        </select>
      </div>

      <div class="d-grid">
        <button type="submit" class="btn btn-primary">Add New Request</button>
      </div>

    </form>
  </div>

</body>
</html>

