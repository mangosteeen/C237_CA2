<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Upcoming Tasks</title>
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #fff8f4;
        }
        .header {
            background-color: #007bff;
            color: white;
            padding: 20px;
            text-align: center;
            border-radius: 5px;
        }
        .task-card {
            background-color: white;
            border-radius: 8px;
            padding: 15px;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
            margin-bottom: 20px;
        }
        .urgency-badge {
            font-size: 0.9rem;
            text-transform: uppercase;
            font-weight: bold;
        }
        .task-header {
            background-color: #d4c6ff;
            color: white;
        }
        .btn-success-custom {
            background-color: #28a745;
            color: white;
            font-weight: bold;
            text-transform: uppercase;
        }
        .task-time {
            font-size: 1rem;
            font-weight: bold;
        }
        .btn-view {
            background-color: #007bff;
            color: white;
        }
        .timeline {
            position: relative;
            margin: 20px 0;
        }
        .timeline .event {
            padding: 10px;
            margin: 15px 0;
            border-left: 2px solid #007bff;
            position: relative;
            padding-left: 40px;
        }
        .timeline .event::before {
            content: '';
            position: absolute;
            left: 0;
            top: 0;
            width: 20px;
            height: 20px;
            border-radius: 50%;
            background-color: #007bff;
        }
    </style>
</head>
<body>

    <div class="container mt-5">
        <!-- Header Section -->
        <div class="header mb-4">
            <h1>Upcoming Tasks</h1>
        </div>

        <!-- Button to Add New Request -->
        <div class="text-right mb-3">
            <a href="/view" class="btn btn-success-custom">Back</a>
        </div>

        <!-- Display Upcoming Tasks Timeline -->
        <div class="timeline">
            <% if (approvedRequests.length > 0) { %>
                <% approvedRequests.forEach(function(request) { %>
                    <div class="event">
                        <h4><%= request.elderName %> - <%= request.taskType %></h4>
                        <p class="task-time"><%= request.date %></p>
                        <p><strong>Description:</strong> <%= request.description %></p>
                        <p><strong>Urgency:</strong>
                            <span class="badge <%= request.urgency === 'High' ? 'badge-danger' : (request.urgency === 'Medium' ? 'badge-warning' : 'badge-success') %> urgency-badge">
                                <%= request.urgency %>
                            </span>
                        </p>
                    </div>
                <% }) %>
            <% } else { %>
                <div class="alert alert-info text-center">No upcoming tasks available.</div>
            <% } %>
        </div>
    </div>

    <!-- Bootstrap JS and dependencies -->
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.5.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
